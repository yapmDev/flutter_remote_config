name: example
description: Example app for flutter_remote_config
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_remote_config:
    path: ../
  http: ^1.6.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0  

flutter:
  uses-material-design: true
