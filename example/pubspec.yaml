name: example
description: Example app for blendberry_flutter_sdk
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  blendberry_flutter_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0  

flutter:
  uses-material-design: true
