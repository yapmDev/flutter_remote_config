name: example
description: Example app for blendberry_flutter_sdk
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  blendberry_flutter_sdk:
    path: ../
  http: ^1.6.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0  

flutter:
  uses-material-design: true
